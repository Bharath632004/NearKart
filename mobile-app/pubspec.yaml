name: nearkart
description: NearKart - Local shop delivery app
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  google_maps_flutter: ^2.5.0
  firebase_messaging: ^14.7.9
  razorpay_flutter: ^1.3.6
  http: ^1.1.0
  geolocator: ^10.1.0
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
